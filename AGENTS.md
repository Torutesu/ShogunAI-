# Agent rules (ShogunAI)

Global rules (communication, pnpm, **model selection**): `/Users/anantsinghal/RULES.md`.

**Hard ban:** NEVER spawn high-fast, `*-fast`, or Grok High Fast — not for chores, not by `inherit` accident. Chore subagents: `model: "composer-2.5"`; never omit `model` if `inherit` would hit a banned tier.

Repo implementation rules: `CLAUDE.md`.
