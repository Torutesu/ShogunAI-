/**
 * Shared domain logic reused across apps (website, desktop, api).
 * The portable referral engine (tokens, tier ladder, masking, validation)
 * currently lives in apps/website/src/lib/referral.ts and can migrate here
 * verbatim — it has no framework or DB dependency.
 */
export {};
