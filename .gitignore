node_modules/
.turbo/
dist/
.next/
out/
build/
*.log
.DS_Store
.env
.env.local

# Rust / Cargo (Phase 0 spike)
/target/
**/target/
# Spike metrics output (JSONL); never commit captured/derived data
/apps/desktop/src-tauri/gen/
*.jsonl
models/

# Local SQLite runtime DBs; never commit — may hold captured/derived user data (不変条件)
*.db
*.db-shm
*.db-wal

# Website build artifacts (OpenNext / Wrangler / Cloudflare)
.open-next/
.wrangler/
