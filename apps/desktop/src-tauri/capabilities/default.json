{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike webview: core event/webview access for the closed IPC contract (spec §3.11.2). Also grants the settings window IPC access for the connections screen.",
  "windows": ["notch", "settings"],
  "permissions": ["core:default"]
}
