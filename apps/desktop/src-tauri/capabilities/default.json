{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Webview access: core IPC plus the window operations the product UI drives. Tauri v2 denies core window APIs unless explicitly allowed — startDragging/setSize were failing SILENTLY with core:default alone (drag and minimize looked dead). The meeting overlay is a separate webview (`meeting`); without it listed here, invoke calls from Recap (transcript, minutes) fail silently while `meeting` events still work.",
  "windows": ["notch", "onboarding", "meeting", "meeting-cc", "meeting-canvas", "meeting-chat", "visual-recall"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "autostart:default"
  ]
}
