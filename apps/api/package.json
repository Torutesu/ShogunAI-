{
  "name": "@shogun-ai/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SHOGUN batch relay (docs/batch-relay-design.md): license-JWT-authenticated relay in front of the Anthropic Batch API. Stores no chunk content — usage aggregates only.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
